import base64

docker_username = base64.b64encode(b'')
docker_password = base64.b64encode(b'')

git_username = base64.b64encode(b'')

print(docker_username)
print(docker_password)
print(git_username)
